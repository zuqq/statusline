[package]
name = "statusline"
version = "0.1.0"
edition = "2021"
description = "Claude Code status line, styled after the Pure zsh prompt"

[dependencies]
serde_json = "1"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
